# ==============================
# Application name
# ==============================
spring.application.name=coursemate

# ==============================
# Database (using Railway env vars)
# ==============================
spring.datasource.url=jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}
spring.datasource.username=${PGUSER}
spring.datasource.password=${PGPASSWORD}

# ==============================
# Server port (Railway sets dynamically)
# ==============================
server.port=${PORT:8080}

# ==============================
# Hibernate / JPA
# ==============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ==============================
# Actuator exposure
# ==============================
management.endpoints.web.exposure.include=health,info
management.endpoint.health.probes.enabled=true
management.endpoint.health.show-details=always
management.health.db.enabled=false

spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1

# Brevo SMTP Configuration (SSL on port 465)
spring.mail.host=smtp-relay.brevo.com
# spring.mail.port=465
spring.mail.username=97b8b0003@smtp-brevo.com   # your Brevo login
spring.mail.password=${BREVO_SMTP_PASSWORD}     # your Brevo SMTP Master Password

spring.mail.port=587
spring.mail.properties.mail.smtp.ssl.enable=false
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

spring.mail.properties.mail.debug=true
logging.level.org.springframework.mail=DEBUG
logging.level.org.springframework.security=INFO

# Timeouts
spring.mail.properties.mail.smtp.connectiontimeout=20000
spring.mail.properties.mail.smtp.timeout=20000
spring.mail.properties.mail.smtp.writetimeout=20000

# Debugging
spring.mail.properties.mail.debug=true
app.mail.from=kumarhirtik3@gmail.com


# Frontend URL (for verification links etc.)
app.frontend.url=https://campusmatefrontend.netlify.app
